<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.sd.pba.LoginActivity" >

    <EditText
        android:id="@+id/un_login"
        android:hint="@string/un_hint"
        android:textColorHint="@color/hint_col"
        android:textColor="@color/txt_col"
        android:background="@color/bkgd_col"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="143dp"
        android:ems="10" >        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/btn_login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/pwd_login"
        android:layout_alignRight="@+id/un_login"
        android:layout_below="@+id/pwd_login"
        android:layout_marginTop="16dp"
        android:text="@string/login"
        android:textColor="@color/txt_col" />

    <EditText
        android:id="@+id/pwd_login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/un_login"
        android:layout_below="@+id/un_login"
        android:layout_marginTop="21dp"
        android:background="@color/bkgd_col"
        android:ems="10"
        android:hint="@string/pwd_hint"
        android:inputType="textPassword"
        android:textColor="@color/txt_col"
        android:textColorHint="@color/hint_col" >
    </EditText>

</RelativeLayout>
